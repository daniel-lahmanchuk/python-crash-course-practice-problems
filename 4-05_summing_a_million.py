# 4-5. Summing a Million: Make a list of the numbers fom one to one million and then use min() and max() to make sure your list actually starts at one and ends at one million. Also, use the sum() function to see how quickly Python adds a million numbers.

numbers = [1, 2, 3, 1,000,000]

min(numbers)
max(numbers)
sum(numbers)